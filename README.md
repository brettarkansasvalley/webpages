# Zucklake Toasty Stack — Git Backup

Code backup of everything behind **https://zucklakeapp.duckdns.org/webapp/** and its
dependencies, gathered on 2026-07-03 from the production server. Originals were not
modified; this tree is a filtered copy (code + configs, no databases/logs/build output).

## Architecture

```
Browser ──HTTPS──► nginx (443, Let's Encrypt, basic auth)
                     │  /webapp/        ──► Flask api.py        :5000  (webapp5000.service)
                     │  /webapp/app.js  ──► static alias to webapp/app.js
                     │  /jaq/           ──► json-loader-server  :3000  (jaq.service)
                     │  /jaq2/          ──► json-loader-server  :3001  (jaq2.service)
                     │  /toasty/        ──► FastAPI app.py      :8001  (manual ./start.sh)
                     └  (other paths belong to unrelated apps — see deploy/nginx/)

Flask webapp (:5000)
  ├─ SQLite:  webapp/webapp.db            (runtime DB — 53 GB, NOT in this repo)
  ├─ reads/writes  toasty/data/{raw,reports}   (runtime data — NOT in this repo)
  ├─ writes JSON to /home/ubuntu/jaq/loadjson/ (data lake — NOT in this repo)
  └─ queries JAQ server at http://localhost:3000 (JAQ_SERVER_URL)

json-loader-server (custom JSON database, Rust/axum + rusqlite + jaq query engine)
  ├─ lives inside a fork of github.com/01mf02/jaq (workspace member, uses jaq-core/std/json crates)
  ├─ jaq/  (:3000) and jaq2/ (:3001) have DIVERGED — jaq2 has extra modules
  │  (app.rs, context.rs, expression.rs, functions.rs, lib.rs, tests, benches)
  └─ each keeps its own database.db (3+ GB — NOT in this repo)
```

## Repo layout

| Path | Source on server | What it is |
|---|---|---|
| `toasty/` | `/home/ubuntu/new_toasty/toasty/` | Flask webapp (`toasty/webapp/`), FastAPI app (`app.py`), Toast API fetchers, `jbhooks/` tip-report tools, `scripts/`, `DSL/`, templates/static |
| `jaq/` | `/home/ubuntu/jaq/` | jaq fork + `json-loader-server/` (port 3000). Upstream `01mf02/jaq` @ commit `1afefb3`; local changes: `json-loader-server/` added as workspace member |
| `jaq2/` | `/home/ubuntu/jaq2/jaq/` | Second, diverged copy of the fork + `json-loader-server/` (port 3001) |
| `deploy/nginx/reverse_proxy` | `/etc/nginx/sites-enabled/reverse_proxy` | Live nginx site config (note: `sites-available/` copy on the server is stale/different) |
| `deploy/systemd/` | `/etc/systemd/system/` | `webapp5000.service`, `jaq.service`, `jaq2.service` |

## Secrets — resolved (2026-07-03)

All credentials were migrated out of code into `.env` files (gitignored). The seven
formerly hardcoded files (`start.sh`, `get_toast.py`, `fetch_date_range.py`,
`toast_30d_report.py`, `webapp/start_api.sh`, `jbhooks/send_email.py`,
`jbhooks/send_fixed_period_report.py`) now read from the environment, loading
`toasty/.env` (and `webapp/.env` for the webapp). See `toasty/.env.example` for the
required keys. On a fresh deploy: copy `.env.example` to `.env` and fill in values.

## Not included (runtime data / rebuildable)

- `webapp/webapp.db` (53 GB), `tip_distribution.db`, jaq `database.db` (3.3 GB each) — SQLite runtime data
- `toasty/data/` (1.7 GB raw + reports), `/home/ubuntu/jaq/loadjson/` (2.5 GB JSON data lake) — regenerated by the fetch/sync scripts
- Rust `target/`, python `.venv`/`__pycache__`, `zig-out`, logs
- `jaq2/.../nanoclaw/` — unrelated third-party tool with its own git repo
- nginx `.htpasswd` files and Let's Encrypt keys

## Restore / run notes

- webapp: `webapp5000.service` runs `webapp/start_api.sh` → python3.14 runs `api.app` on :5000; needs `TOAST_*` env vars and `JAQ_SERVER_URL`
- jaq servers: `cargo build --release -p json-loader-server` inside each fork; systemd units point at `target/release/json-loader-server`
- python deps: `toasty/requirements.txt` (plus flask, flask-cors, apscheduler, requests, werkzeug used by `webapp/api.py`)
