# ============================================================
# SECRETS — all credentials now live in .env files (see
# toasty/.env.example); the former hardcoded-credential files
# were scrubbed on 2026-07-03 and are tracked normally.
# ============================================================
.env
*.env
!.env.example
.htpasswd*

# Runtime data & databases (never commit — webapp.db alone is 53 GB)
*.db
*.db.*
*.sqlite
data/
loadjson/

# Logs & pids
*.log
*.pid

# Build artifacts & caches
__pycache__/
*.pyc
.venv/
venv/
node_modules/
dist/
target/
.zig-cache/
zig-out/
.ruff_cache/
toasty/fetch_date_range.py
toasty/toast_30d_report.py
